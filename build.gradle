// Root project for plugin-registry multi-project build

group 'io.seqera'
version "${file('VERSION').text.trim()}"

// Common configuration for subprojects that don't use custom plugins
allprojects {
    group 'io.seqera'
    version rootProject.version
}

// Wrapper task
wrapper {
    gradleVersion = '8.5'
}
