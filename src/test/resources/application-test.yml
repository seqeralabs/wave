micronaut:
  http:
    client:
      read-timeout: 90s

towerreg:
  arch: x86_64

  registries:
    test:
      host: changed at runtime
      auth:
        username: test
        password: test
        url: changed at runtime
        service: localhost
